{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "prepare": "husky",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "test:links": "node tests/garland-maps-url.test.mjs && node tests/dallas-maps-url.test.mjs && node tests/contact-details.test.mjs && node tests/maps-url-live.test.mjs",
    "test:healthchecks": "node tests/healthchecks.test.mjs"
  },
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
