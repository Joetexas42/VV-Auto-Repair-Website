name = "vv-auto-repair-website"
compatibility_date = "2024-01-01"

[site]
bucket = "./artifacts/vv-auto-website"

# ──────────────────────────────────────────────────────────────
# Cloudflare Pages deployment
# ──────────────────────────────────────────────────────────────
# Deploys are handled automatically by GitHub Actions.
# See .github/workflows/deploy.yml — every push to main deploys
# artifacts/vv-auto-website to the Cloudflare Pages project
# "vv-auto-repair-website".
#
# Manual one-off deploy:
#   npx wrangler pages deploy artifacts/vv-auto-website --project-name=vv-auto-repair-website
# ──────────────────────────────────────────────────────────────
