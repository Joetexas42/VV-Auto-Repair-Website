/**
 * @deprecated All map and review URLs are now served exclusively by the API
 * server via the /api/location-config endpoint (see artifacts/api-server/src/routes/locationConfig.ts).
 * Override them at runtime with the DALLAS_MAPS_URL, GARLAND_MAPS_URL,
 * DALLAS_WRITE_REVIEW_URL, and GARLAND_WRITE_REVIEW_URL environment variables.
 *
 * Do not add hardcoded URL constants here — edit the API server or set env vars instead.
 */
