{
  "name": "@workspace/vv-auto-website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve . --listen tcp://0.0.0.0:$PORT --no-clipboard",
    "deploy": "wrangler pages deploy . --project-name vv-auto --commit-dirty=true"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "wrangler": "^3.0.0"
  }
}
